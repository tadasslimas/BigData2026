matplotlib>=3.7.0
seaborn>=0.12.0
pytest>=7.3.0
psutil>=5.9.0
folium>=0.14.0
pyspark>=3.4.0
pandas>=2.0.0
pyarrow>=10.0.0

# Optional: Enhanced sorting performance on macOS
# Install gsort for better memory management during vessel proximity analysis:
#   macOS: brew install coreutils
#   Linux: gsort is typically pre-installed as 'sort'
#   Windows: Use WSL or install Git Bash for sort command
